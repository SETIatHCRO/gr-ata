# auto-generated by grc.converter

id: ata_atacontrol
label: ATA Control
category: '[ATA]'

parameters:
-   id: ipaddr
    label: Server IP
    dtype: string
    default: localhost
-   id: port
    label: Server Port
    dtype: int
    default: '4321'
-   id: polling_sec
    label: Polling Freq (s)
    dtype: int
    default: '20'
-   id: antenna
    label: Antenna Name
    dtype: string
    default: ant1
-   id: tuning
    label: Tuning (LO)
    dtype: string
    default: a
-   id: verbose
    label: Verbose
    dtype: bool
    default: 'False'
    options: ['False', 'True']
    option_labels: ['No', 'Yes']

inputs:
-   domain: message
    id: control
    optional: true

outputs:
-   domain: message
    id: freq
    optional: true
-   domain: message
    id: status
    optional: true

templates:
    imports: import ata
    make: ata.atacontrol(${ipaddr}, ${port}, ${polling_sec}, ${tuning}, ${antenna},
        ${verbose})

file_format: 1
