# auto-generated by grc.converter

id: ata_ata_stream
label: ATA Stream
category: '[ATA]'

parameters:
-   id: port
    label: UDP Port
    dtype: int
    default: '50000'
-   id: header
    label: Header Format
    dtype: int
    default: '4'
    options: ['4', '5']
    option_labels: [New Header, Old Header]
-   id: payloadsize
    label: UDP Packet Data Size
    dtype: int
    default: '4160'
-   id: notifyMissed
    label: Notify Missed Frames
    dtype: enum
    options: ['False', 'True']
    option_labels: ['No', 'Yes']

outputs:
-   domain: stream
    dtype: byte

templates:
    imports: import grnet
    make: grnet.udp_source(gr.sizeof_char, 1, ${port},${header},${payloadsize},${notifyMissed},False)

documentation: |-
    This block is a convenience wrapper for a gr-grnet UDP source block specific to ATA data streams.  Data streams come in on two UDP ports: 50000 for x polarization and 50001 for y polarization.  The new packet format has a 4160 payload (4096 + a 64-bit header).

file_format: 1
